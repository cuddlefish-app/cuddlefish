- name: api
  definition:
    # url: ${API_GRAPHQL_ENDPOINT}
    # See https://docs.docker.com/docker-for-mac/networking/#use-cases-and-workarounds.
    url: http://host.docker.internal:3001/graphql
    # Use a relatively long timeout for lengthy git checkouts.
    timeout_seconds: 600
